import { View, Text } from 'react-native';

export default function mdp() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>Ici feature pour modifier mon mot de passe</Text>
    </View>
  );
}
