import { View, Text } from 'react-native';

export default function photo() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 20 }}>Ici feature pour modifier ma photo</Text>
    </View>
  );
}
